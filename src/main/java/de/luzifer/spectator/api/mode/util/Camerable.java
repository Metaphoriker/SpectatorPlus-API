package de.luzifer.spectator.api.mode.util;

/**
 * Interface to assign a camera to a SpectateMode
 *
 * Will automatically add a camera to the Spectator using this mode
 *
 * The interface has no functions. It serves purely for tagging purposes
 * Just like if(mySpectateMode instanceof Camerable) {do}
 */
public interface Camerable {}
